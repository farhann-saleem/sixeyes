# Taste — Marketing Studio (Farhann Saleem)
- **Large, readable font sizes everywhere** — repeatedly complains text is "too small" and asks to increase font sizes across the entire product (headings, navbar, body, smaller text). Never accepts tiny/unreadable type. Confidence: 0.95
- **Centered navbar with library on the right side** — navbar text should be centered, "Your Films"/library moved to the right side of the navbar, and navbar font increased ~2x. Keep navbar size consistent with the landing page. Confidence: 0.9
- **Beige and navy color blend with proper shading** — background uses a blend of beige and navy (not black), with proper shading and highlighting. Avoid dim, low-contrast text and color issues. Confidence: 0.9
- **Premium, consistent UI with no "AI slop"** — wants high-quality, consistent styling across all pages with proper buttons, shading, cards, and animations. No cookie-cutter, generic AI aesthetics. Keep the existing theme but fix inconsistencies in box styling, shadows, and highlights. Confidence: 0.9
- **Messy grid layouts in some sections, clean grids in others** — effects and landing pages should use messy/offset grid formations with multiple columns and asymmetrical layouts. Library should use a clean, normal 4-grid layout. Don't leave everything center-aligned. Confidence: 0.85
- **No horizontal scrolling on any page** — all pages should fit within the viewport width with no horizontal overflow. Confidence: 0.95
- **Loading animations during generation** — use smooth loading animations (like Gemini/ChatGPT style) while creating images or videos, rather than static "processing" text. Confidence: 0.85
- **Videos autoplay wherever they appear** — "where ever there is a video play it" — background videos and demo videos should autoplay to give a clean, sleek look. Confidence: 0.85
- **Prompt injection security in all prompt boxes** — every prompt input must be prompt-guarded: strip hidden characters, refuse jailbreak/role-override attempts, and treat user topic text as data (not executable instruction). Confidence: 0.95
- **Prevent API key/secret leaks** — never commit `.env` or any secrets to git. Rotate exposed keys immediately. Use proper `.gitignore` in every repo. Verify no secrets are pushed. Confidence: 0.95
- **Security-conscious about provider-side secret access** — cares whether the cloud host (AWS EC2) can read runtime API keys/secrets on the box, i.e., the provider trust boundary, not just whether keys leak to git/GitHub. Confidence: 0.5
- **Cloud rendering over local CPU** — video/rendering work should happen on cloud servers (Modal, RunPod) rather than the user's local machine. The user explicitly mentions being a "low RAM user" and wants cloud-based compilation. Confidence: 0.85
- **Prefers RunPod as the backend/production host** — when the app needs a public host, the user reaches for RunPod first (asked to host the backend on RunPod, and whether a serverless endpoint would work). Already has a RunPod account, so RunPod CPU pods are the expected fast deployment target. Confidence: 0.7
- **Agent capture is mandatory** — all work must be automatically captured via hooks (`.cursor/hooks/capture.py` or equivalent) into `.agent-logs/`. The `.agent-logs/` directory ships with the repo (not gitignored). Must work across sessions on its own, no manual logging. Confidence: 0.9
- **Differentiate clearly from Higgsfield** — the product must clearly state what's unique vs. Higgsfield. Even though it clones the Higgsfield experience, it needs a distinguishing factor. Keep the "Marketing Studio / Your Imagination Engine" branding. Confidence: 0.85
- **Move fast and stay concise under time pressure** — when the user signals they're short on time ("be fast as much you can", "be quick", "be quick as fast as you can", "fix this as fast as possible"), ship the work quickly with minimal ceremony and minimal back-and-forth rather than lengthy explanations. Confidence: 0.9
- **Git-push-to-deploy workflow** — tests locally, then expects pushing to GitHub to auto-render the live site (Vercel-style). Wants simple, automated deploy over manual steps, and needs the assistant to explain/fix production routing (rewrites, backend URL) rather than handling infra solo. Confidence: 0.6
- **Wants an explicit go/no-go before pushing to GitHub** — asks "am I good to go?" and expects the assistant to have already verified gitignore coverage, scanned staged/new files for secrets, and confirmed tests/build pass, then give a clear green light rather than just summarizing what changed. Confidence: 0.6
- **Thoroughly verify code before shipping** — when the user asks to "verify all possible things you can so we don't have a wrong code or function," do a full verification pass (test flows, typecheck, build, security hardening) rather than a quick spot-check. Wants the assistant to proactively catch and fix bugs before anything goes out. Confidence: 0.8
- **Terse, high-level instructions with implied scope** — the owner writes casual shorthand (typos included, e.g. "updaet the pricing and each and evrything - and set rate limits and everything") and expects the agent to infer the full scope and fill in unspecified details (like rate-limit numbers) with sensible defaults rather than asking for a complete spec. Confidence: 0.75
- **Treat owner-provided figures as locked constants** — exact values the owner hands over (pricing table, quotas, multipliers) are implemented verbatim: don't round, reinterpret, or invent. Only fill in what wasn't specified. Confidence: 0.7
- **SwichNow (Pakistani payment gateway) for checkout** — billing runs through SwichNow's hosted checkout (Landing Page/PWA flow) in PKR with HMAC checksums; USD price shown with a PKR equivalent for paid tiers. Confidence: 0.6
- **Public browsing, auth only for create/write actions** — landing pages and all content must be viewable by everyone without signing in; Google auth is required only when the user tries to create/generate (write actions), not gated across the whole app. Confidence: 0.85
- **Disable auth/friction for local demo recording** — when recording a demo video or testing locally, prefers auth and sign-in prompts turned off entirely (guards disabled, navbar auth UI removed) so nothing interrupts the flow; expects it fully restored afterward (backend guards, navbar sign-in badge, /callback handler, and create-authorize flow all re-enabled). Confidence: 0.8

- **Prefers parallel agents to speed up work** — when asked to do several things quickly, the user explicitly asks to "set parallel agents" to parallelize the work rather than doing everything sequentially. Confidence: 0.8
dalone Node backend** — the frontend deploys to Vercel (with a `vercel.json` `/api/*` rewrite); the backend is a separate Node/Express service on port 3001 hosted independently (RunPod/Render), wired to the frontend via that rewrite plus a `FRONTEND_URL` env var. Treat them as two deployable units. Confidence: 0.7
